# Core Dependencies
rich>=13.0.0
pydantic>=2.0.0
loguru>=0.7.0
pyyaml>=6.0
requests>=2.31.0

# Database & Async
aiosqlite>=0.19.0

# Special Features (Used in Tour)
apprise>=1.7.0
psutil>=5.9.0
tqdm>=4.66.0

# Data & Vision (Used in some examples)
numpy>=1.24.0
opencv-python-headless>=4.8.0

# WPipe (Installing from source in Binder via postBuild or current repo)
.
